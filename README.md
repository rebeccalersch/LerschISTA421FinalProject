To run the code in the script, simply ensure that the SATDATA.csv file is in the directory that you are running the Lersch421Final.py in.

There should be no other things needed to successfully run the contents of this. 
